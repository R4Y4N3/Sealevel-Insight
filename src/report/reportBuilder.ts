import { WorkspaceReport } from '../model/report';

export function reportHtml(report: WorkspaceReport, nonce: string): string {
  const json = JSON.stringify(report).replace(/</g, '\\u003c');
  return `<!doctype html><html><head><meta charset="UTF-8"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'nonce-${nonce}'; script-src 'nonce-${nonce}';"><style nonce="${nonce}">body{font-family:var(--vscode-font-family);color:var(--vscode-foreground);background:var(--vscode-editor-background);padding:24px;max-width:1100px;margin:auto}h1{font-size:24px}h2{margin-top:28px;border-bottom:1px solid var(--vscode-panel-border);padding-bottom:8px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px}.metric{padding:12px;background:var(--vscode-textCodeBlock-background);border:1px solid var(--vscode-panel-border)}.value{font-size:22px;font-weight:600;display:block}table{border-collapse:collapse;width:100%}td,th{text-align:left;padding:8px;border-bottom:1px solid var(--vscode-panel-border)}</style></head><body><h1>Sealevel Insight</h1><div id="app"></div><script nonce="${nonce}">const report=${json};const app=document.getElementById('app');const s=report.summary;const metrics=[['Programs',report.programs.length],['Rust files',s.rustFiles],['LOC',s.loc],['nSLOC',s.codeLoc],['Functions',s.functions],['Instructions',s.instructions],['Accounts',s.accounts],['PDAs',s.pdas],['CPIs',s.cpis],['PDA-signed CPIs',s.pdaSignedCpis]];app.innerHTML='<h2>Workspace</h2><div class="grid">'+metrics.map(([n,v])=>'<div class="metric">'+n+'<span class="value">'+v+'</span></div>').join('')+'</div>'+report.programs.map(p=>'<h2>'+p.name+'</h2><p>'+p.frameworkEvidence.map(f=>f.framework+' ('+Math.round(f.confidence*100)+'%): '+f.evidence.join(', ')).join('<br>')+'</p><h3>Functions</h3><table><tr><th>Name</th><th>LOC</th><th>Complexity</th><th>Parameters</th></tr>'+p.functions.map(f=>'<tr><td>'+f.name+'</td><td>'+f.lines+'</td><td>'+f.complexity+'</td><td>'+f.parameters+'</td></tr>').join('')+'</table><h3>Security Surface</h3><div class="grid">'+[['Signer signals',p.securitySurface.signerSignals],['Writable signals',p.securitySurface.writableSignals],['Raw/unchecked',p.securitySurface.rawOrUncheckedAccounts],['Remaining accounts',p.securitySurface.remainingAccounts],['Manual owner checks',p.securitySurface.manualOwnerChecks],['Unsafe blocks',p.securitySurface.unsafeBlocks]].map(([n,v])=>'<div class="metric">'+n+'<span class="value">'+v+'</span></div>').join('')+'</div>').join('');</script></body></html>`;
}
